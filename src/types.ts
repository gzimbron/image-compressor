export interface FolderCompressOptions {
	quality?: number;
	maxWidth?: number;
	maxHeight?: number;
	extensions?: string[];
	pngToWebp?: boolean;
}

export interface ImageCompressOptions {
	quality?: number;
	maxWidth?: number;
	maxHeight?: number;
	pngToWebp?: boolean;
}
